/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
  domains: ['d1muf25xaso8hp.cloudfront.net' , 'img.daisyui.com'  ], // Add the hostname here
}
};

export default nextConfig;
